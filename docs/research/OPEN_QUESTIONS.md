# Open Questions

- Which local embedding model should be default for first docs-kb iteration?
- Should session persistence use SQLite-only initially or hybrid file-plus-db?
- What is the first minimal event contract required for CLI + Web parity?
- How should domain-level permissions be represented across profiles and tools?
- What quality gates should be mandatory before enabling autonomous execution waves?
