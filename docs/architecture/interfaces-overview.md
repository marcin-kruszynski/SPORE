# Interfaces Overview

The future SPORE implementation should converge on a small set of stable interfaces:

- configuration loading,
- runtime session startup,
- event publication and subscription,
- documentation retrieval,
- client query and command surfaces,
- review and completion reporting.

Each interface should be driven by explicit contracts that are inspectable in files and schemas, not hidden in prompt text alone.
