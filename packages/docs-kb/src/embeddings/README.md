# Embeddings

This module now contains the local embedding provider abstraction used by the bootstrap implementation.

Current default:

- `local-hash-v1`

This provider creates deterministic local vectors without external dependencies. It is intentionally lightweight and replaceable.
