id: parallel-any-investigation
name: Parallel Any Investigation
triggerType: manual
applicableProjectTypes: [platform, application, service]
roleSequence: [lead, scout, builder, reviewer]
stepSets:
  - name: framing
    roles: [lead]
    gate:
      mode: all
    policy:
      maxActiveMs: 20000
      onTimeout: hold_execution
      onFailure: fail_execution
      blockNextWaveOnOpenEscalation: true
  - name: exploratory-split
    roles: [scout, builder]
    gate:
      mode: any
    policy:
      maxActiveMs: 25000
      onTimeout: open_escalation
      onFailure: open_escalation
      blockNextWaveOnOpenEscalation: false
  - name: synthesis
    roles: [reviewer]
    gate:
      mode: all
    policy:
      maxActiveMs: 15000
      onTimeout: hold_execution
      onFailure: fail_execution
      blockNextWaveOnOpenEscalation: true
branchingConditions:
  - if: evidence-conflicts
    then: request-branch-rework
reviewStep:
  required: true
retryPolicy:
  maxAttempts: 2
documentationUpdatePolicy:
  required: true
completionRequirements:
  - findings-documented
  - review-verdict-recorded
