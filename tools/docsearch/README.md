# Docsearch

This folder defines the bootstrap search strategy for SPORE documentation.

Use this area to design a local-first search layer that starts simple, stays replaceable, and can be used by humans and agents.

The first working slice is now implemented in `packages/docs-kb/` with a SQLite-backed local index and stable CLI commands.
